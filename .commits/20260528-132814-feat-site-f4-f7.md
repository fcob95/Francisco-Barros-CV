# Commit 20260528-132814

**Type**: feat
**Scope**: app / components / lib (analytics, seo, contact) / i18n messages
**Triviality**: non-trivial
**Validated by user**: yes

## Summary

feat: integrate design v1 + animation + analytics + SEO (F4–F7)

## What changed

- **F4 — design integration** (presentational + container, ported by value from
  design-assets/v1, never imported): Header/Footer site-wide, Hero (home), Projects
  list + detail (`/projects`, `/projects/[slug]` SSG), About (`/about`, real photo
  via next/image), Experience (`/experience` timeline + education), Contact
  (`/contact` form, RHF+Zod, localized field errors + honeypot) with a Resend API
  route (`/api/contact`, shared Zod schema, no persistence), Privacy (`/privacy`),
  editorial 404. Section i18n keys added to messages with ES/EN parity.
- **F5 — animation**: `app/[locale]/template.tsx` page transition; global
  `prefers-reduced-motion` reset + print CSS in globals.css.
- **F6 — analytics**: PostHog governed by a blocking consent banner; typed event
  catalog + `track.*` helpers (single capture chokepoint, no-op without consent);
  delegated `data-event` listener + scroll/time hooks; opt-out; privacy-safe config.
- **F7 — SEO**: dynamic per-page metadata, canonical + hreflang, sitemap, robots,
  JSON-LD (Person/CreativeWork), dynamic OG images via next/og; one `<h1>` per page.
- Cleanup: removed `theme-toggle`/`locale-switcher`/`ui/dropdown-menu` (superseded by
  the ported Header) and the `@radix-ui/react-dropdown-menu` dep; eslint ignores
  Playwright artifacts. Added user assets: CV PDFs + avatar photo.

## Files modified

- `app/[locale]/**`: new section pages + layout/page/not-found edits, template,
  opengraph-image routes; `app/api/contact/route.ts`; `app/{sitemap,robots}.ts`;
  `app/globals.css`.
- `components/sections/**`, `components/analytics/**`, `components/seo/JsonLd.tsx`,
  `components/cookie-banner/**`; deletions of the superseded toggles + dropdown.
- `lib/analytics/**`, `lib/contact/schema.ts`, `lib/seo/**`.
- `messages/{es,en}.json`: section/header/cta/hero/footer/contact/privacy/cookies keys.
- `e2e/{home,pages}.spec.ts`; `eslint.config.mjs`; `package.json` + `pnpm-lock.yaml`;
  `.gitignore` (+ .env.local); `public/cv/*`, `public/images/avatar.jpeg`.

## Implementation notes

- Asset callbacks (onNavigate/onClick) replaced by next-intl `<Link>` so routing is
  locale-correct and sections stay Server Components where possible — marked with
  `// DESIGN-DEVIATION`. Contact swapped native validation for RHF+Zod (shared schema
  client↔API). next/og fonts: local → CDN → Satori-default fallback (never throws).
  PostHog raw-IP discard is a manual PostHog project setting (cannot be forced client
  side) — documented in TODO_MANUALES §3.1.

## Tests

- Vitest: 57 passing (content + analytics consent gate + SEO URL logic).
- e2e: 10/10 against the production build (`CI=1 pnpm test:e2e`).
- Full quartet green: typecheck / lint / build / test.

## Risks / Notes

- `/api/contact` has Zod + length caps + honeypot but NO rate-limiting (accepted for
  v1; see PROGRESS). Orphan public SVGs (old generic project visuals, avatar_base,
  experience svgs) and SECURITY_AUDIT.md remain — handled in a follow-up cleanup.
- Numbered editorial labels (e.g. "01 / Home") stay English in ES by design — pending
  Francisco's confirmation.
