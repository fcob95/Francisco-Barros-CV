# Commit 20260528-132906

**Type**: docs
**Scope**: PROGRESS / STATUS / TODO_MANUALES
**Triviality**: non-trivial
**Validated by user**: yes

## Summary

docs: record F4–F7 completion + manual setup tasks

## What changed

- `PROGRESS.md`: state updated to F4–F7 complete (scope through F7 done, site
  production-ready, not deployed); decision 14 (ADR-008) recorded; open notes for
  deferred items (h1 done in F7, contact rate-limiting deferral, orphan SVGs to
  clean, email unification, English editorial labels pending confirmation).
- `STATUS.md`: phase board updated to F4–F7 ✅; next = F8 (deploy) out of scope.
- `TODO_MANUALES.md`: new deliverable — step-by-step tasks only Francisco can do
  (Resend, PostHog incl. §3.1 raw-IP toggle, profile data, CV/avatar marked done,
  email unification, F8 domain/Vercel).

## Files modified

- `PROGRESS.md`, `STATUS.md`: source-of-truth state/decision updates.
- `TODO_MANUALES.md`: new file (manual setup guide).

## Implementation notes

None — documentation only. Architectural decisions live here (PROGRESS), not in
the per-commit logs, per the source-of-truth hierarchy.

## Tests

N/A (docs).

## Risks / Notes

- A repo-hygiene/cleanup pass is queued next (formalize already-done deletions —
  done in commits e563ea8/495ec74 — plus remove orphan public SVGs, add LICENSE/
  .editorconfig, dead-code/deps sweep). SECURITY_AUDIT.md left untracked pending the
  public-repo decision.
