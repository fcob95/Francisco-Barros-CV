# Commit 20260526-142536

**Type**: docs
**Scope**: STATUS.md, PROGRESS.md
**Triviality**: trivial
**Validated by user**: yes (cierre de fase + decisión locale ratificada)

## Summary

docs: close F3 in STATUS/PROGRESS, ratify locale-detection (#5), set F4 as next

## What changed

- Marcado F3 ✅ cerrada (PR #4) en ambos docs rectores; foco movido a F4 (integración de diseño).
- Ampliada la decisión #5: `localeDetection: true` ratificado — `/` canónico ES, navegadores EN
  redirigen a `/en`; "ES raíz" = ES sin prefijo (no "siempre español en `/`").
- Notas abiertas: candidatos de limpieza en F4 (DropdownMenu/locale-switcher si el Header de v1 los
  hace obsoletos; mover toggles a `components/layout/`) y nota de JS budget (gzipped holgado).

## Files modified

- `PROGRESS.md`: encabezado de estado (F3 ✅, siguiente F4), decisión #5 ampliada, ítem `[x]` de F3,
  consolidación de "Notas abiertas" (evitado heading duplicado).
- `STATUS.md`: encabezado, fila E3, "Estado de fases" y fila F3 de la tabla.

## Implementation notes

Solo documentación de estado; refleja trabajo ya hecho y revisado (commit del feature F3 en esta misma
rama). La ratificación de #5 no cambió código: `localeDetection` ya estaba en su default `true`.

## Tests

None (documentación).

## Risks / Notes

None.
